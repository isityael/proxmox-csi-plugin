when:
  - event: tag
    ref: refs/tags/v*-ym.*
    path:
      include:
        - go.mod
        - go.sum
        - cmd/**
        - pkg/**
        - tools/**
        - Dockerfile
        - Makefile
        - .woodpecker/ci.yaml
        - .woodpecker/release.yaml
        - .ci/test-release-policy.sh

steps:
  # Controller and node images share only the Dockerfile builder stage, so
  # build them in parallel (DAG mode); later steps chain explicitly.
  - name: build-controller-candidate
    depends_on: []
    image: woodpeckerci/plugin-docker-buildx:6.1.2@sha256:d26b69cbe748b8a219c707e7889d5ffb3f9214bc3c480c30fe857ff85954267c
    privileged: true
    settings:
      registry: ghcr.io
      repo: ghcr.io/isityael/proxmox-csi-controller
      dockerfile: Dockerfile
      target: proxmox-csi-controller
      platforms: [linux/amd64]
      tags: ["candidate-${CI_COMMIT_SHA}"]
      cache_from: 'type=registry\,ref=ghcr.io/isityael/proxmox-csi-controller:buildcache'
      cache_to: 'type=registry,ref=ghcr.io/isityael/proxmox-csi-controller:buildcache,mode=max,ignore-error=true'
      build_args:
        TAG: "${CI_COMMIT_TAG}"
        SHA: "${CI_COMMIT_SHA}"
      logins:
        - registry: ghcr.io
          username: {from_secret: github_username}
          password: {from_secret: github_token}
        - registry: dhi.io
          username: {from_secret: dhi_username}
          password: {from_secret: dhi_password}
      provenance: mode=max

  - name: build-node-candidate
    depends_on: []
    image: woodpeckerci/plugin-docker-buildx:6.1.2@sha256:d26b69cbe748b8a219c707e7889d5ffb3f9214bc3c480c30fe857ff85954267c
    privileged: true
    settings:
      registry: ghcr.io
      repo: ghcr.io/isityael/proxmox-csi-node
      dockerfile: Dockerfile
      target: proxmox-csi-node
      platforms: [linux/amd64]
      tags: ["candidate-${CI_COMMIT_SHA}"]
      cache_from: 'type=registry\,ref=ghcr.io/isityael/proxmox-csi-node:buildcache'
      cache_to: 'type=registry,ref=ghcr.io/isityael/proxmox-csi-node:buildcache,mode=max,ignore-error=true'
      build_args:
        TAG: "${CI_COMMIT_TAG}"
        SHA: "${CI_COMMIT_SHA}"
      logins:
        - registry: ghcr.io
          username: {from_secret: github_username}
          password: {from_secret: github_token}
        - registry: dhi.io
          username: {from_secret: dhi_username}
          password: {from_secret: dhi_password}
      provenance: mode=max

  - name: scan-candidates
    depends_on: [build-controller-candidate, build-node-candidate]
    image: dhi.io/trivy:0.74.0-alpine3.24-dev@sha256:c0fe6badae7fa331254b3ea3b53ea1e74e7067ce5aa63f6f05e40538963d97b6
    environment:
      TRIVY_USERNAME: {from_secret: github_username}
      TRIVY_PASSWORD: {from_secret: github_token}
    commands:
      - trivy image --exit-code 1 --severity HIGH,CRITICAL --ignore-unfixed "ghcr.io/isityael/proxmox-csi-controller:candidate-$${CI_COMMIT_SHA}"
      - trivy image --exit-code 1 --severity HIGH,CRITICAL --ignore-unfixed "ghcr.io/isityael/proxmox-csi-node:candidate-$${CI_COMMIT_SHA}"

  - name: sign-candidates
    depends_on: [scan-candidates]
    image: dhi.io/cosign:3.1.3-alpine3.24-dev@sha256:e72a27089bde22daa2cc8653011ef55c4a8ecfb7cc5271df62325cd661d77eff
    environment:
      COSIGN_EXPERIMENTAL: "1"
      COSIGN_KEY: {from_secret: cosign_key}
      COSIGN_PASSWORD: {from_secret: cosign_password}
      GHCR_USERNAME: {from_secret: github_username}
      GHCR_TOKEN: {from_secret: github_token}
    commands:
      - printf '%s' "$${COSIGN_KEY}" > /tmp/cosign.key
      - printf '%s' "$${GHCR_TOKEN}" | cosign login ghcr.io -u "$${GHCR_USERNAME}" --password-stdin
      - cosign sign --key /tmp/cosign.key --yes --registry-referrers-mode=oci-1-1 "ghcr.io/isityael/proxmox-csi-controller:candidate-$${CI_COMMIT_SHA}"
      - cosign sign --key /tmp/cosign.key --yes --registry-referrers-mode=oci-1-1 "ghcr.io/isityael/proxmox-csi-node:candidate-$${CI_COMMIT_SHA}"
      - rm -f /tmp/cosign.key

  - name: promote-release
    depends_on: [sign-candidates]
    image: dhi.io/crane:0.21.9-debian13-dev@sha256:3fb565e1e3aef17a5f7bf9bb697e88cdad502f94b775f8998db7ac94ee164994
    environment:
      GHCR_USERNAME: {from_secret: github_username}
      GHCR_TOKEN: {from_secret: github_token}
    commands:
      - printf '%s' "$${GHCR_TOKEN}" | crane auth login ghcr.io -u "$${GHCR_USERNAME}" --password-stdin
      - short_sha="$$(printf '%s' "$${CI_COMMIT_SHA}" | cut -c1-8)"
      - crane copy "ghcr.io/isityael/proxmox-csi-controller:candidate-$${CI_COMMIT_SHA}" ghcr.io/isityael/proxmox-csi-controller:edge
      - crane copy "ghcr.io/isityael/proxmox-csi-controller:candidate-$${CI_COMMIT_SHA}" "ghcr.io/isityael/proxmox-csi-controller:${CI_COMMIT_TAG}"
      - crane copy "ghcr.io/isityael/proxmox-csi-controller:candidate-$${CI_COMMIT_SHA}" "ghcr.io/isityael/proxmox-csi-controller:$${short_sha}"
      - crane copy "ghcr.io/isityael/proxmox-csi-node:candidate-$${CI_COMMIT_SHA}" ghcr.io/isityael/proxmox-csi-node:edge
      - crane copy "ghcr.io/isityael/proxmox-csi-node:candidate-$${CI_COMMIT_SHA}" "ghcr.io/isityael/proxmox-csi-node:${CI_COMMIT_TAG}"
      - crane copy "ghcr.io/isityael/proxmox-csi-node:candidate-$${CI_COMMIT_SHA}" "ghcr.io/isityael/proxmox-csi-node:$${short_sha}"
